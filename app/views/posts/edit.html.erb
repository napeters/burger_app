<h1>Edit your post</h1>

<div>
  <%= form_for [ @user, @post ] do |f| %>
    <%= render partial: 'form', locals: {f: f} %>
    <p>
      <%= f.submit "Save" %>
    </p>
  <% end %>
</div>
<p class="back">
  <%= link_to "<< Nevermind", user_post_path, {:class => "cancel"} %>
</p>
